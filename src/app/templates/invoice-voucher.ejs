<!doctype html>
<html>
  <head>
    <meta charset="UTF-8" />
    <style>
      body {
        background-color: #f1f5f9;
        font-family:
          -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, Arial,
          sans-serif;
        padding: 24px;
      }

      .card {
        max-width: 620px;
        margin: auto;
        background: #ffffff;
        border-radius: 14px;
        padding: 28px;
        box-shadow: 0 10px 25px rgba(0, 0, 0, 0.06);
      }

      /* Header */
      .header {
        display: flex;
        justify-content: space-between;
        align-items: center;
        margin-bottom: 14px;
      }

      .header h2 {
        margin: 0;
        color: #0b3a67;
        font-size: 22px;
      }

      .badge {
        background: #e0ecf8;
        color: #0b3a67;
        padding: 6px 14px;
        border-radius: 999px;
        font-size: 13px;
        font-weight: 600;
        white-space: nowrap;
      }

      /* Text */
      .muted {
        color: #6b7280;
        font-size: 14px;
        line-height: 1.6;
        margin: 6px 0;
      }

      .label {
        color: #374151;
        font-size: 15px;
        line-height: 1.6;
      }

      hr {
        border: none;
        border-top: 1px solid #e5e7eb;
        margin: 20px 0;
      }

      /* Rows */
      .row {
        display: flex;
        justify-content: space-between;
        align-items: center;
        font-size: 15px;
        margin-bottom: 8px;
      }

      .value {
        font-weight: 500;
      }

      /* Total */
      .total {
        background: linear-gradient(135deg, #0b3a67, #0e4c8a);
        color: #ffffff;
        padding: 22px;
        border-radius: 12px;
        margin-top: 18px;
      }

      .total .row {
        margin: 0;
        font-size: 17px;
      }

      /* Button */
      .btn-wrapper {
        text-align: center;
      }

      .btn {
        display: inline-block;
        margin-top: 26px;
        background: #0b3a67;
        color: #ffffff;
        text-decoration: none;
        padding: 14px 30px;
        border-radius: 10px;
        font-weight: 600;
        font-size: 15px;
      }

      /* Footer */
      .footer {
        margin-top: 22px;
        text-align: center;
        font-size: 13px;
        color: #9ca3af;
      }
    </style>
  </head>

  <body>
    <div class="card">
      <div class="header">
        <h2>Invoice Ready</h2>
        <span class="badge">#<%= invoiceNo %></span>
      </div>

      <p class="muted">
        Issued on
        <strong>
          <%= new Date(invoiceDate).toLocaleDateString('en-GB') %>
        </strong>
      </p>

      <hr />

      <p class="muted">Payable To</p>
      <p class="label">
        <strong><%= payableTo.name %></strong><br />
        <%= payableTo.address || '' %><br />
        <%= payableTo.phone || '' %>
      </p>

      <hr />

      <div class="row">
        <span>Subtotal</span>
        <span class="value"><%= subTotal.toLocaleString() %> tk</span>
      </div>

      <div class="row">
        <span>Discount</span>
        <span class="value">- <%= discount.toLocaleString() %> tk</span>
      </div>

      <div class="row">
        <span>Tax</span>
        <span class="value"><%= tax.toLocaleString() %> tk</span>
      </div>

      <div class="total">
        <div class="row">
          <strong>Grand Total</strong>
          <strong><%= grandTotal.toLocaleString() %> tk</strong>
        </div>
      </div>

      <div class="btn-wrapper">
        <a href="<%= pdfUrl %>" target="_blank" class="btn">
          View / Download Invoice
        </a>
      </div>

      <div class="footer">
        If you have any questions, feel free to reach out anytime.
      </div>
    </div>
  </body>
</html>
